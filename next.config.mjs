/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '',
  assetPrefix: 'https://vaqm2.github.io/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
